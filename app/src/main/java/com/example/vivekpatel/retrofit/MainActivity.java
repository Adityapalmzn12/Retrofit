package com.example.vivekpatel.retrofit;import android.os.Bundle;import android.support.v7.app.AppCompatActivity;import android.util.Log;import android.view.View;import android.widget.Button;import android.widget.TextView;import retrofit2.Call;import retrofit2.Callback;import retrofit2.Response;import retrofit2.Retrofit;import retrofit2.converter.gson.GsonConverterFactory;public class MainActivity extends AppCompatActivity{    TextView text;    public static String baseurl="https://reqres.in/";    static Retrofit retrofit=null;    ResultModel resultModel;    Button btn;    @Override    protected void onCreate(Bundle savedInstanceState)    {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        text=(TextView)findViewById(R.id.text);        btn=(Button)findViewById(R.id.btn);        final UserModel userModel=new UserModel("vivek","cse");        retrofit= new Retrofit.Builder().baseUrl(baseurl).addConverterFactory(GsonConverterFactory.create()).                build();        final ApiInterface apiInterface=retrofit.create(ApiInterface.class);        final Call<ResultModel>call=apiInterface.insert_user(userModel);//        call.enqueue(new Callback<DataModel>()//        {////            @Override//            public void onResponse(Call<DataModel> call, Response<DataModel> response)//            {//                resultModel=response.body().getData();////                text.setText(String.valueOf(resultModel.getId())//                +resultModel.first_name+resultModel.last_name);////            }////            @Override//            public void onFailure(Call<DataModel> call, Throwable t)//            {//                t.printStackTrace();////            }//        });        btn.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View view)            {                Callback<ResultModel>callback=new Callback<ResultModel>() {                    @Override                    public void onResponse(Call<ResultModel> call, Response<ResultModel> response)                    {                        text.setText(String.valueOf(response.body().getId()));                        Log.d("---------","-------------");                    }                    @Override                    public void onFailure(Call<ResultModel> call, Throwable t)                    {                        t.printStackTrace();                        Log.d("---------------------","----------------");                    }                };                apiInterface.insert_user(userModel);                call.enqueue(callback);            }        });    }}